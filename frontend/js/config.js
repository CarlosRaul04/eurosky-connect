// frontend/js/config.js
//
// Configuracion del cliente. Si pones tu clave de Google Maps JavaScript API,
// el mapa usara Google Maps; si la dejas vacia, se usa un mapa SVG de respaldo
// que dibuja las coordenadas reales de los aeropuertos (funciona sin clave).
window.EUROSKY_CONFIG = {
    // Pega aqui tu clave para activar Google Maps. Ej: 'AIzaSy...'
    googleMapsApiKey: '',

    // Base de la API REST. Vacio = mismo origen que sirve el frontend.
    apiBase: '',
};